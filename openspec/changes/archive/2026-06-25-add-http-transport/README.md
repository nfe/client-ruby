# add-http-transport

Camada HTTP zero-dependência sobre `Net::HTTP` (stdlib): transport `Nfe::Http`, value objects `Request`/`Response`, retry com backoff + jitter, hierarquia de erros tipados e `ErrorFactory`, roteamento multi-base-URL, logger duck-typed com redação de chaves.
